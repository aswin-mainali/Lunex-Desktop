# Command schema

`POST /commands/route`

```json
{ "command": "open calculator", "confirmed": false }
```

Intents: `open_app`, `open_website`, `web_search`, `file_search`, `summarize_document`, `system_status`, `create_note`, `unknown`.
